- $click = ((defined? click) ? click: "remove()")

- $label = ((defined? label) ? label: "删除")

- if !(defined? disabled)
  %button.btn.btn-circle.red-sunglo.btn-sm{:type => "button", 'ng': {'click':"#{$click}"}}
    %i.fa.fa-plus
    #{$label}

- if defined? disabled
  %button.btn.btn-circle.red-sunglo.btn-sm{:type => "button", 'ng': {'click':"#{$click}", 'disabled': "#{disabled}"}}
    %i.fa.fa-plus
    #{$label}
