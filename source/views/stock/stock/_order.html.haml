-#订单列表
%table#sample_editable_1.table.table-striped.table-hover.table-bordered
  %thead
    %tr
      - ['编号','商品名称','零散比','正装数量','散装数量','供应商','电话','手机','更新时间'].each do |fieldName|
        %th #{fieldName}

  %tbody
    %tr.odd.gradeX{ "ng-repeat" => "order in orders"}
      - [0,1,2,3,4,5,6,7].each do |fieldName|
        %td {{order[#{fieldName}]}}
      %td {{order[8]| date:'yyyy-MM-dd HH:mm:ss'}}

%center
  = partial :'layouts/buttons/btn', :locals => {:label=>"打印", :click => "stockportal.print()"}